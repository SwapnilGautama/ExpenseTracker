# 💰 Personal Expense Tracker

A simple Streamlit-based personal expense tracker that allows users to:

- Log daily expenses
- Categorize spending
- Set a monthly budget
- Track spending against the budget
- Automatically save and load data

## 🚀 How to Run Locally

1. Clone the repository:
   git clone https://github.com/YOUR_USERNAME/personal-expense-tracker.git

2. Navigate to project folder:
   cd personal-expense-tracker

3. Install dependencies:
   pip install -r requirements.txt

4. Run the app:
   streamlit run app.py
